/**
 * Game Bridge — Pure utility functions for backend API interaction.
 *
 * This module contains NO module-level state. All state is managed
 * by OnlineAwareGameProvider via React context.
 */

export { api } from '../api/client';
